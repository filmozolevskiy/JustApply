# Ralph Loop Progress Log

## Iteration 2 — Issue #75: Russian-only filtered enrichment (Russian Apify filter + non-HR cap)

**Implemented:**
- Added `RUSSIAN_SAMPLE_SIZE = 5`, `RUSSIAN_SEARCH_QUERY = "Russian"`, `RUSSIAN_EXCLUDE_FUNCTION_IDS = ["12"]` constants to `contact_sample.py`.
- Extended `_fetch_apify_employees_at_url` with `search_query` and `exclude_function_ids` parameters, wired into `actor_input` as `searchQuery` and `excludeFunctionIds`.
- Added `_run_apify_for_russian_speakers()` which calls Apify with `searchQuery="Russian"`, `excludeFunctionIds=["12"]`, `maxItems=5`.
- Updated `source_contacts()` to detect Russian-only mode (`target_russian_speakers=True, target_recruiters=False`) and route to `_source_stream(stream="russian", ...)`, importing `_run_apify_for_russian_speakers` and `RUSSIAN_SAMPLE_SIZE`.
- Updated `_source_stream()` to dispatch `_run_apify_for_russian_speakers` when `stream == "russian"`.
- Fixed `classifier.py` so the Russian Speaker pool excludes Recruiter-classified contacts (`add_russian` requires `not is_recruiter_classified`). Dual-classified contacts still show both `russian_speaker=True` and `is_recruiter=True` badges, but count toward Recruiter cap only — consistent with CONTEXT.md.
- Added 14 new tests in `tests/test_russian_stream.py` covering constants, Russian-only orchestration (mocked Apify), per-stream cache hit/miss/empty caching, infrastructure error propagation, log messages, non-HR cap enforcement, and `_fetch_apify_employees_at_url` Apify input verification.
- All 501 tests pass.

## Iteration 1 — Issue #73: Recruiter-only filtered enrichment (per-stream cache + Apify HR filter)

**Implemented:**
- Migrated `contact_sample_cache` DB table from single `company_slug PRIMARY KEY` to compound `(company_slug, stream)` PRIMARY KEY, with migration for existing installs.
- Updated all cache layer functions (`get_contact_sample`, `set_contact_sample`, `append_contact_sample`, `delete_contact_sample`) to accept a `stream` parameter (default `''`).
- Added `RECRUITER_FUNCTION_IDS = ["12"]` and `RECRUITER_SAMPLE_SIZE = 3` constants to `contact_sample.py`.
- Added `function_ids` and `max_items` parameters to `_fetch_apify_employees_at_url` so audience-specific Apify filters can be passed.
- Added `_run_apify_for_recruiters()` which calls Apify with HR function filter and maxItems=3.
- Updated `source_contacts()` to detect recruiter-only mode (`target_recruiters=True, target_russian_speakers=False`) and route to a per-stream cache path (`stream='recruiters'`) using the new filtered Apify function. Legacy path (both or neither active) is unchanged.
- Fixed classifier recruiter cap from 5 → 3 (`RECRUITER_CAP = 3`); Russian speaker cap remains 5.
- Added 19 new tests in `tests/test_recruiter_stream.py` covering cache schema, recruiter-only orchestration (mocked Apify), stream isolation, cap enforcement.
- All 487 tests pass.


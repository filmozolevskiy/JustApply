# Ralph Loop Progress Log

## Iteration 1 — Issue #24: Contact schema + Apify normalization extension

Added `is_recruiter: bool = False` to the `Contact` model in `src/schemas.py`.

Extended `_normalize_apify_employee` in `src/core/outreach.py` to extract two new fields from raw Apify profiles: `currentPosition` and `location` (defaulting to `""` when absent). Also initialises `is_recruiter: False` on every normalized contact.

Added 4 new tests (TDD red→green):
- `test_schemas.py`: `test_contact_schema_has_is_recruiter_field`
- `test_outreach.py`: `test_normalize_extracts_current_position_and_location`, `test_normalize_handles_missing_current_position_and_location`, `test_contact_new_fields_persist_through_db_roundtrip`

All 93 tests pass. No regressions.

## Iteration 2 — Issue #25: Outreach Settings model + API

Added `OutreachSettings` Pydantic model to `src/schemas.py` with `target_russian_speakers: bool = True` and `target_recruiters: bool = True`.

Created `src/db/settings.py` with `get_outreach_settings` and `save_outreach_settings` functions using SQLite upsert (ON CONFLICT DO UPDATE).

Updated `src/db/connection.py` to create the `outreach_settings` table in `init_db`.

Updated `src/db/__init__.py` to export the two new functions.

Added `GET /api/settings/outreach` and `PUT /api/settings/outreach` endpoints to `src/web/server.py`.

All 3 new tests pass (TDD red→green). Full suite: 96 tests pass, no regressions.

# Ralph Loop Progress Log

## Iteration 4 — Issue #27: Audience-specific outreach message templates

Replaced the single `build_outreach_prompt` function with two audience-specific template builders in `src/core/outreach.py`:
- `build_russian_speaker_prompt` — English greeting, includes referral program ask and offer to share CV
- `build_recruiter_prompt` — English greeting, direct connect/share-CV ask, no referral mention

Updated `generate_outreach_message` to accept an `is_recruiter: bool = False` parameter and select the correct prompt builder (recruiter takes priority over russian_speaker when both are True). Removed all Cyrillic text from the fallback template (no more `Добрый день`).

Updated `generate_outreach_for_job` to read `is_recruiter` from the primary contact and pass it through to `generate_outreach_message`.

Updated 2 existing tests that asserted Cyrillic in output. Added 7 new tests (TDD red→green):
- `test_build_russian_speaker_prompt_contains_referral_and_english_greeting`
- `test_build_recruiter_prompt_no_referral_direct_connect`
- `test_generate_outreach_message_fallback_no_cyrillic_for_russian_speaker`
- `test_generate_outreach_message_gemini_uses_recruiter_prompt_when_is_recruiter`
- `test_generate_outreach_message_gemini_uses_russian_speaker_prompt_when_is_russian`
- `test_generate_outreach_message_recruiter_priority_over_russian_speaker`
- `test_generate_outreach_for_job_passes_is_recruiter_flag`

Full suite: 108 tests pass, no regressions.

## Iteration 1 — Issue #24: Contact schema + Apify normalization extension

Added `is_recruiter: bool = False` to the `Contact` model in `src/schemas.py`.

Extended `_normalize_apify_employee` in `src/core/outreach.py` to extract two new fields from raw Apify profiles: `currentPosition` and `location` (defaulting to `""` when absent). Also initialises `is_recruiter: False` on every normalized contact.

Added 4 new tests (TDD red→green):
- `test_schemas.py`: `test_contact_schema_has_is_recruiter_field`
- `test_outreach.py`: `test_normalize_extracts_current_position_and_location`, `test_normalize_handles_missing_current_position_and_location`, `test_contact_new_fields_persist_through_db_roundtrip`

All 93 tests pass. No regressions.

## Iteration 2 — Issue #25: Outreach Settings model + API

Added `OutreachSettings` Pydantic model to `src/schemas.py` with `target_russian_speakers: bool = True` and `target_recruiters: bool = True`.

Created `src/db/settings.py` with `get_outreach_settings` and `save_outreach_settings` functions using SQLite upsert (ON CONFLICT DO UPDATE).

Updated `src/db/connection.py` to create the `outreach_settings` table in `init_db`.

Updated `src/db/__init__.py` to export the two new functions.

Added `GET /api/settings/outreach` and `PUT /api/settings/outreach` endpoints to `src/web/server.py`.

All 3 new tests pass (TDD red→green). Full suite: 96 tests pass, no regressions.

## Iteration 3 — Issue #26: Batch LLM contact classification (classify_contacts)

Added `classify_contacts(items, settings)` async function to `src/core/outreach.py`.

The function sends a single batch Gemini prompt with all raw Apify profiles formatted as numbered entries. Gemini returns a JSON array of classified contacts with `russian_speaker` and `is_recruiter` flags. Results are capped at 5 per audience type. Contacts matching neither type are excluded. Settings (`OutreachSettings`) control which audience types are included in the output.

Added `import json` to `src/core/outreach.py`.

Added 5 new tests (TDD red→green) to `tests/test_outreach.py`:
- `test_classify_contacts_assigns_russian_speaker_flag`
- `test_classify_contacts_assigns_recruiter_flag`
- `test_classify_contacts_handles_dual_classified_contact`
- `test_classify_contacts_caps_at_five_per_group`
- `test_classify_contacts_returns_empty_when_llm_returns_no_matches`

Full suite: 101 tests pass, no regressions.

# Ralph Loop Progress — PRD #113

## 2026-07-06 — Issue #147: Versioned migration runner

**Task:** Introduce versioned migration runner for Job Tracker Database (#147)

**Summary:**
- Added `src/db/migrations.py` with numbered migrations (v1–v9) tracked via `PRAGMA user_version`
- Refactored `init_db()` to call `run_migrations()` instead of ad-hoc ALTER try/except chain
- Preserved seeding guard, post-seed status/rejectedAt backfills, and contacted-profiles index hook
- Added `tests/test_migrations.py` for fresh init, legacy upgrade, idempotency, and batch_jobs table

**Quality gates:** ruff ✓ | mypy ✓ | pytest 892 passed, 84.67% coverage ✓

**Commit:** Add versioned migration runner for Job Tracker Database (#147)

## 2026-07-06 — Issue #148: Remove in-database Job Backup from domain glossary

**Task:** Remove in-database Job Backup from domain glossary (#148)

**Summary:**
- Removed **Job Backup** entry from `CONTEXT.md` (no `jobs_backup` table was ever implemented)
- Updated **Database Snapshot** to state it is the sole disaster-recovery path; kept `jobs_backup` only in `_Avoid_` terms
- Updated `docs/prd/contacted-elsewhere-performance.md` to index live `jobs` contacts only
- Added `tests/test_context_glossary.py` asserting glossary hygiene via TDD

**Quality gates:** ruff ✓ | mypy ✓ | pytest 894 passed, 84.72% coverage ✓

**Commit:** Remove in-database Job Backup from domain glossary (#148)

## 2026-07-06 — Issue #149: SQLite WAL mode and busy timeout

**Task:** Enable SQLite WAL mode and busy timeout on database connections (#149)

**Summary:**
- Updated `get_db_connection` to set `PRAGMA journal_mode=WAL` and `PRAGMA busy_timeout=5000` on each open
- Added module comment explaining WAL reduces lock contention for dashboard reads overlapping CLI/enrichment writes
- Added `tests/test_connection.py` asserting WAL and busy timeout via TDD

**Quality gates:** ruff ✓ | mypy ✓ | pytest 896 passed, 84.73% coverage ✓

**Commit:** Enable SQLite WAL mode and busy timeout on database connections (#149)

## 2026-07-07 — Issue #150: Document migration failure recovery via Database Snapshot

**Task:** Document migration failure recovery via Database Snapshot (#150)

**Summary:**
- Added **Migration Failure Recovery** glossary entry to `CONTEXT.md` with step-by-step restore from `~/.just_apply/backups/`
- Clarified snapshot timing (before **Destructive Database Operation**, at `--search`/`--promote` start) and contrast with removed in-DB **Job Backup**
- Linked versioned migration runner (`PRAGMA user_version`, `src/db/migrations.py`) in recovery flow
- Added agent cross-link in `CLAUDE.md` MUST section pointing to CONTEXT.md recovery steps
- Extended `tests/test_context_glossary.py` via TDD for recovery docs and CLAUDE.md link

**Quality gates:** ruff ✓ | mypy ✓ | pytest 898 passed, 84.73% coverage ✓

**Commit:** Document migration failure recovery via Database Snapshot (#150)

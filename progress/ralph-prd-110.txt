# Ralph Loop Progress — PRD #110

## #144 — Add contacted-profiles index table and backfill from existing jobs

- Added `contacted_profiles` SQLite table (profile_slug, job_id, company, title, contacted_at)
- `ensure_contacted_profiles_index()` runs on `init_db`: creates table and backfills when missing/empty
- Backfill reuses `build_contacted_index()` with same `/in/{slug}` normalization; includes archived jobs
- Skips legacy rows that cannot be parsed into full Job models (minimal-schema fixtures)
- Tests: backfill after DROP TABLE upgrade path; URL variant normalization
- Quality gates: ruff, mypy, pytest (880 passed, 84.83% coverage)

## #145 — Keep contacted-profiles index in sync on contact and enrichment writes

- Added `sync_job_contacted_profiles_index()` for incremental per-job index updates (no full rescan)
- Hooked into `update_contact_status` (Kanban contact checkbox toggle) and `enrich_job` (enrich, load-more, re-classify persist paths)
- Marking contacted upserts index row with contacted_at/company/title; unmarking removes job/profile pair
- Re-classify contact rewrites replace prior index rows for that job
- Tests: toggle add/remove index rows; enrich and reclassify sync without rebuild
- Quality gates: ruff, mypy, pytest (884 passed, 84.81% coverage)

## #146 — Serve Contacted Elsewhere from index on job reads with regression guard

- `enrich_jobs_with_contacted_elsewhere` now uses `load_contacted_index_for_slugs()` (batched IN query on `contacted_profiles`) instead of `_load_all_jobs` + `build_contacted_index`
- `_lookup_slugs_for_jobs()` collects slugs only from non-contacted contacts on returned jobs
- Hooked `add_job` into index sync so jobs inserted with contacted contacts populate the index (needed for read path correctness)
- Regression tests spy on `_load_all_jobs` to ensure `get_job` and `get_jobs` never reintroduce full-table scans
- Quality gates: ruff, mypy, pytest (886 passed, 84.71% coverage)

## Loop complete

- Verified no open `ready-for-agent` issues for Parent PRD #110 (only slices #144, #145, #146; all closed)
- Open `ready-for-agent` queue contains PRD #113 slices only (#147–#150)
- Status: <promise>COMPLETE</promise>

# Ralph Loop Progress — PRD #95

Started: 2026-06-26
Composed skills: tdd

## Iteration 1 — #100 Reusable Spend Confirmation modal + convert Apify dialogs

Status: DONE
Commit: 4f6cca9

### Summary
- Added reusable `#spend-confirmation-modal` styled to match the Kanban Dashboard (amber spend accent, stream list, cost estimate).
- `showSpendConfirmModal()` — promise-based confirm (true/false); dismiss via Cancel, backdrop, or Esc.
- `showSpendAckModal()` — single-button acknowledgement variant (no Cancel) for blocked notices.
- Converted **Enrich Job** cache-miss gate and **Load More Contacts** preflight confirm from native `confirm()` to the modal.
- Converted load-more blocked path from native `alert()` to `showSpendAckModal()`.
- Re-classify unchanged (no confirmation).
- Updated static client tests in `test_cost_confirmations.py` and `test_load_more_stream_aware.py`.

### Tests
- 756 passed (full suite)

## Iteration 2 — #102 Region-scoped scrape backend + Bright Data per-region payload

Status: DONE
Commit: a59570e

### Summary
- `POST /api/search` now accepts structured `search_regions` (country + region pairs) and `per_region_limit` instead of free-text `location`.
- Server validates ≥1 Search Region per selected country; rejects `"Remote"` and unknown regions with 422.
- `per_region_limit` clamped to [25, 1000] server-side before scrape runs.
- Bright Data trigger payload builds one input item per (country, region) with `location`, `country`, and `limit_per_input`.
- Added `validate_search_regions`, `clamp_per_region_limit` in `src/core/regions.py`; refactored scraper payload builder.
- New tests in `tests/test_region_scoped_search.py`; updated existing `/api/search` test payloads.

### Tests
- 766 passed (full suite)

## Iteration 3 — #103 Job Search Settings: country-scoped region pickers + Per-Region Limit stepper

Status: DONE
Commit: 8e16bdb

### Summary
- Removed free-text Location field; `"Remote"` can no longer be set as a scrape location.
- Added country-scoped Search Region pickers rendered from `GET /api/regions`; checking a country reveals only that country's regions.
- Run Scraper button starts disabled with a hint until each checked country has ≥1 region selected.
- Added Per-Region Limit stepper (25–1000, step 25, default 200) with client-side clamping via `clampPerRegionLimit()`.
- `triggerScrapeRun()` sends structured `search_regions` and `per_region_limit` to `POST /api/search` (no `location` field).
- New static client tests in `tests/test_job_search_settings_regions.py`.

### Tests
- 777 passed (full suite)

## Iteration 4 — #104 Scrape Spend Confirmation (checkout-receipt) wired into scrape flow

Status: DONE
Commit: 2b4c0fc

### Summary
- Wired checkout-receipt Spend Confirmation into `triggerScrapeRun()` before `POST /api/search`.
- Left panel: keyword, Search Regions grouped by country, time range.
- Right panel: itemized cost ceiling with editable Per-Region Limit stepper; live recompute of Max postings and Max spend.
- Cancel/backdrop/Esc closes modal without scrape; confirm runs scrape and syncs limit back to `#kb-per-region-limit`.
- Deleted throwaway prototype `src/web/prototypes/spend-confirmation.prototype.html`.
- New static client tests in `tests/test_scrape_spend_confirmation.py`.

### Tests
- 788 passed (full suite)

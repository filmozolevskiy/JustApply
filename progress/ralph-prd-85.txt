# Ralph Loop Progress — PRD #85

## Iteration 1 — #96 Profile Manager modal foundation (browse + active selection)
- Added Manage Profiles button (top-right nav) opening two-pane Profile Manager modal
- Lists profiles from GET /api/resumes; selecting shows markdown in read-only editor
- Active badge + Set active; persists to localStorage (ACTIVE_RESUME_KEY)
- resolveActiveResume falls back to first profile when stored value missing/invalid
- Removed toolbar Active Profile dropdown and read-only View modal
- Tests: tests/test_dashboard_profile_manager.py (9 static HTML/JS scan tests)
- Full suite: 733 passed

## Iteration 2 — #97 Author Resume Profiles (edit existing + create blank + save)
- POST /api/resumes: update existing by filename (verbatim) or create new with sanitized snake_case name
- Collision on new profile appends timestamp suffix (e.g. qa_<timestamp>.md); original untouched
- Profile Manager: editable textarea, Save wired to API, New opens review state with name input + banner
- Set active / Delete disabled during review; editor sync preserves in-progress edits on re-render
- Tests: tests/test_dashboard_resumes.py (+3 backend), test_dashboard_profile_manager.py (+4 static)
- Full suite: 740 passed

## Iteration 3 — #98 Delete Resume Profiles (guarded against active / last remaining)
- DELETE /api/resumes/{filename}?active_resume=… removes non-active profile .md file
- Rejects delete of Active Resume Profile (409) and last remaining profile (409)
- Profile Manager: delete button with confirm(); enabled only when deletable
- Client guards mirror server; row removed from list on success
- Tests: tests/test_dashboard_resumes.py (+3 backend), test_dashboard_profile_manager.py (+2 static)
- Full suite: 745 passed

## Iteration 4 — #99 Resume Import (PDF to Resume Profile via Gemini multimodal)
- POST /api/resumes/convert: accepts PDF upload, calls generate_text_from_pdf with standardized prompt, returns markdown only (no disk write)
- Rejects non-PDF (422), missing GEMINI_API_KEY (503), LLM failure (503); raw PDF never persisted
- Profile Manager: Import PDF file picker, spinner during conversion, review state pre-filled with converted markdown
- Added python-multipart dependency for multipart uploads
- Tests: tests/test_dashboard_resumes.py (+4 backend), test_dashboard_profile_manager.py (+3 static)
- Full suite: 752 passed

# Ralph Loop Progress — PRD #206

Started: Job Links PRD with TDD composition.
Queue frontier: #209 → #210 → #211 (parent #206).
Approved seams (from PRD): HTTP TestClient for `GET /jobs/{id}`; pure Job Link path helpers; prior-art wiring/source tests. Do not re-quiz seams.

## #209 Job Link happy path — open, close, reload (closed)
- Added pure `jobLinks.js` helpers: parse/build `/jobs/{id}`, board root, push history when path changes.
- `GET /jobs/{id}` serves the same dashboard HTML as `/` without shadowing `/api` or `/static`.
- Opening a drawer pushes `/jobs/{id}`; closing pushes `/`; boot after `loadJobs` reopens when the job is already on the board.
- Tests: `tests/test_job_links.py` (helpers + HTTP + wiring); quality gates green (ruff, mypy, pytest cov).
- Next frontier: #210 (blocked by #209 until close).

## #210 Job Link history, navigation sync, and cold open (closed)
- Added `ensureJobLoaded` + `applyJobLinkPath` in drawer: cold-fetch via `GET /api/jobs/{id}`, upsert, open; missing id closes drawer, `pushBoardRootHistory`, Task Log error.
- Boot and `popstate` call `applyJobLinkPath` (no extra history push when path already matches).
- Prev/next and Contacted Elsewhere keep URL sync through `openJobDetailsDrawer` / shared cold-load helper.
- Tests: `tests/test_job_links.py` (#210 wiring); quality gates green (ruff, mypy, pytest cov).
- Next frontier: #211 (Unsaved Draft Warning on Job Link URL leave; blocked by #210 until close).

## #211 Unsaved Draft Warning on Job Link URL leave (closed)
- `applyJobLinkPath` runs `confirmDiscardIfNeeded` before URL leave/switch (Back/Forward/path change).
- Cancel restores the still-open Job Link via `restoreJobLinkHistory` (replaceState); Discard clears drafts and completes navigation.
- Widened `get_drawer_body` window so outreach `/200` counter tests still see drawer HTML after Job Link growth.
- Tests: `tests/test_job_links.py` (#211 wiring + restore helper); quality gates green (ruff, mypy, pytest cov).
- Queue for PRD #206: no further ready-for-agent child slices.

# Ralph Loop Progress — PRD #138

## Iteration 1 — Company Research — first-fetch end-to-end (Matched+ drawer) (#139)

**Status:** closed

**Summary:**
- Added `company_research_cache` table and `companyResearch` JSON column on `jobs`; extended **Job** model parsing.
- Promoted Glassdoor Apify logic into `src/core/company_research/` (search/overview/salaries/interviews, LinkedIn slug tie-break, job snapshot builder).
- Added preflight `GET /api/jobs/{id}/company-research-preflight` and background run `POST /api/jobs/{id}/company-research` with Matched+ lane guard, slice-level billable/cached breakdown, cache + job snapshot persistence, Task Log streaming, and Job Activity Log entries.
- Wired drawer **Glassdoor Company Research** section (Variant A metrics grid), spend modal with editable Glassdoor job title, and `researchCompany()` orchestration.
- CLI script `scripts/glassdoor_company_intel.py` now delegates to core module.
- Tests: cache module, Glassdoor core, API/pipeline, drawer/spend-modal static assertions.

**Quality gates:** ruff ✓ · mypy ✓ · pytest 859 passed · coverage 84.59%

**Commit:** 0dedf7d

## Iteration 2 — Company Research — cache-aware reuse and Refresh (#140)

**Status:** closed

**Summary:**
- Added `effective_glassdoor_job_title()` so preflight/run default to the job's stored **Glassdoor job title** on refresh (not the listing title).
- Preflight and run endpoints use existing `companyResearch.glassdoorJobTitle` when no override is passed.
- `researchCompany()` passes stored title to preflight so full cache hits skip **Spend Confirmation** on refresh.
- Tests: preflight full/partial hit math, partial pipeline (title slices only), infrastructure error not cached, two jobs same employer different titles, drawer Refresh + spend-skip wiring.

**Quality gates:** ruff ✓ · mypy ✓ · pytest 866 passed · coverage 84.76%

**Commit:** 20be118

## Iteration 3 — Company Research — Wrong company repick (#141)

**Status:** closed

**Summary:**
- Added `POST /api/jobs/{id}/company-research/candidates` — clears employer **Company Research Cache**, runs Glassdoor `companySearch`, returns top 3 picker candidates.
- Added `GET .../repick-preflight` and `POST .../repick` with background task; `run_company_research_repick_pipeline` seeds picked employer then fetches billable overview/title slices.
- Repick logs **Job Activity Log** as `Company research · employer changed to {matchedName}`.
- Drawer **Wrong company?** button opens employer picker modal; spend confirmation when repick is billable; task log streaming mirrors refresh flow.
- Core helpers: `format_search_candidates`, `format_repick_activity_log_message`, `resolve_repick_slices`.

**Quality gates:** ruff ✓ · mypy ✓ · pytest 875 passed · coverage 84.68%

**Commit:** c40f05f

## Iteration 4 — Company Research — remove UI prototype route and assets (#142)

**Status:** closed

**Summary:**
- Removed `/prototype/company-research` FastAPI route and `PROTOTYPE_COMPANY_RESEARCH_HTML` constant from `server.py`.
- Deleted throwaway prototype artifacts: `company-research.html`, `NOTES.md`, `prototype-company-research.css`, `companyResearchUiPrototype.js`.
- Production drawer unchanged — Variant A markup/CSS lives in `companyResearchUi.js` and `dashboard.css`.
- Tests: prototype route 404, deleted file assertions, no server references; existing drawer wiring tests still pass.

**Quality gates:** ruff ✓ · mypy ✓ · pytest 878 passed · coverage 84.73%

**Commit:** (pending)

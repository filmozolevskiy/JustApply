# Ralph Loop Progress — PRD #111

## #125 — Introduce pyproject.toml with runtime and dev dependency groups
- Added `pyproject.toml` as canonical metadata: runtime deps (FastAPI stack, google-genai, httpx, pydantic, dotenv, multipart, watchfiles, sse-starlette) and `[dev]` extras (pytest, pytest-asyncio, pytest-cov, ruff, mypy) with compatible-release bounds.
- Regenerated `requirements.txt` as runtime-only backward-compatible re-export (removed pytest).
- Updated README setup: `pip install -e ".[dev]"` for contributors, runtime-only `pip install -e .`, documented pin policy (no lock file).
- Tests: 817 passed.
- Commit: e8211ea

## #126 — Add Ruff lint gate with baseline passing on src/
- Added `[tool.ruff]` to `pyproject.toml`: py311 target, line-length 100, E/F/I/UP rules with E501 deferred; per-file ignores for `__init__.py` re-exports and `cli.py` load_dotenv import order.
- Auto-fixed unused imports, import sorting, and pyupgrade-safe modernizations across `src/` and `tests/`; removed stale `database` re-export from `src/cli/__init__.py`.
- Documented `ruff check src tests` in README (Quality checks) and CLAUDE.md command table.
- Tests: 817 passed; `ruff check src tests` exits 0.
- Commit: e2443b9

## #127 — Add mypy type gate on core packages
- Added `[tool.mypy]` to `pyproject.toml`: py311, `follow_imports = "silent"`, scoped to `src/db`, `src/schemas.py`, `src/service`; pragmatic `ignore_missing_imports` for google/httpx/sse_starlette stubs.
- Fixed three type errors in scoped packages: guard `job.id is None` in `contacted_elsewhere.py`; normalize `allowed_remote_types` default to `["any"]` in `just_apply.py` before pipeline calls.
- Documented `mypy` (no args) in README Quality checks and CLAUDE.md command table.
- Tests: 817 passed; `mypy` exits 0 (12 source files).
- Commit: 017d530

## #128 — Add pytest-cov coverage threshold for backend src/
- Measured baseline: 84.61% on `src/` Python (3658 stmts, 563 miss); term report rounds to 85%.
- Added `[tool.pytest.ini_options]` (`asyncio_mode = auto`), `[tool.coverage.run]` (source `src`, omit dashboard HTML/JS), and `[tool.coverage.report]` (`fail_under = 84`) to `pyproject.toml`.
- Documented `pytest tests/ --cov=src --cov-report=term-missing` in README Quality checks and CLAUDE.md command table.
- Tests: 817 passed with coverage gate; threshold 84% reached (84.61%).
- Commit: e5cdd23

## #129 — Add GitHub Actions CI for tests and quality gates
- Added `.github/workflows/ci.yml`: triggers on PRs and pushes to `main`; Python 3.11 with pip cache; installs `pip install -e ".[dev]"`.
- Runs same local commands: `ruff check src tests`, `mypy`, `pytest tests/ --cov=src --cov-report=term-missing` as separate steps (non-zero exit fails workflow).
- No secrets required; no application behavior changes.
- Tests: 817 passed; ruff, mypy, coverage gate (84.55%) all exit 0 locally.
- Commit: 001c822

## #130 — Document quality verification commands in CLAUDE.md
- Split CLI commands from a dedicated **Quality checks** section in `CLAUDE.md`: CI-matching gate order (`ruff check src tests`, `mypy`, `pytest tests/ --cov=src --cov-report=term-missing`) with dev install prerequisite (`pip install -e ".[dev]"`, see README).
- Updated AI workflow step 3: agents must run the full gate suite on Python changes, not pytest alone.
- Tests: 817 passed.
- Commit: 6992420

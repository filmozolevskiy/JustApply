# Ralph Loop Progress — PRD #114

Started: 2026-07-02
Parent PRD: #114 (Kanban Dashboard Maintainability)
Queue: 7 ready-for-agent issues (#131–#137)

## #131 — Move Job Tracker Database init to FastAPI startup and add health smoke test
- Removed module-import `init_db()` from `src/web/server.py`; runs in startup hook alongside Batch Poller (skipped under pytest via `PYTEST_CURRENT_TEST`)
- Added `tests/test_server_lifecycle.py`: import does not init DB, `/api/health` smoke test, startup creates jobs table
- Quality gates: ruff, mypy, pytest (820 passed, 84.47% coverage)
- Closed #131

Remaining queue: #132–#137

## #132 — Remove sys.path hacks from dashboard web entrypoints
- Removed `sys.path.insert` from `src/web/server.py` and `src/web/run_dashboard.py`; package-relative imports already resolve via `python3 -m src.web.run_dashboard`
- Simplified `run_dashboard.py` to use `Path(__file__).resolve().parents[2]` for reload dirs
- Added `test_web_modules_do_not_mutate_sys_path` in `tests/test_server_lifecycle.py`
- Quality gates: ruff, mypy, pytest (821 passed, 84.67% coverage)
- Closed #132

Remaining queue: #133–#137

## #133 — Prune completed SSE active_tasks after terminal state and TTL
- Added `TASK_PRUNE_TTL_SECONDS` (default 60s, overridable via env) and `_schedule_active_task_prune` to remove terminal tasks after SSE disconnect + TTL when no streams remain
- Track `stream_count` on `TaskState`; schedule prune from SSE `finally` block for completed/failed tasks
- Fixed reconnect replay: terminal tasks with empty queue no longer block on `queue.get()` during TTL window
- Added pruning unit and integration tests in `tests/test_log_stream.py`
- Quality gates: ruff, mypy, pytest (826 passed, 84.76% coverage)
- Closed #133

Remaining queue: #134–#137

## #134 — Consolidate duplicate job scrape HTTP endpoints behind one handler
- Extracted `_evaluation_lock_response` and `_trigger_scrape_task` shared helpers in `src/web/server.py`; both `/api/search` and `/api/scrape` route through the same task trigger logic
- Marked `/api/scrape` as `deprecated=True` in OpenAPI with description pointing callers to POST `/api/search`
- Added OpenAPI deprecation and shared-handler response-shape tests in `tests/test_server_lifecycle.py`
- Quality gates: ruff, mypy, pytest (828 passed, 84.76% coverage)
- Closed #134

Remaining queue: #135–#137

## #135 — Extract Kanban dashboard CSS into static stylesheet
- Moved ~2,050 lines of inline CSS from `dashboard.html` to `src/web/static/css/dashboard.css`
- HTML shell links `/static/css/dashboard.css`; no inline `<style>` block remains
- Added `read_dashboard_css()` helper in `tests/kanban_js.py`; updated lane collapse, DnD, and module tests to read CSS from the static asset
- Added `test_dashboard_links_stylesheet` and `test_server_serves_dashboard_stylesheet`
- Quality gates: ruff, mypy, pytest (830 passed, 84.76% coverage)
- Closed #135

Remaining queue: #136–#137

## #136 — Extract board orchestration and search client JS into static modules
- Extracted inline JS into focused ES modules: `perRegionLimit.js`, `spendConfirmation.js`, `evaluationLock.js`, `jobSearchSettings.js`, `boardOrchestration.js`, `dashboardApp.js`
- HTML shell reduced to minimal bootstrap importing `dashboardApp.js`; Profile Manager and outreach settings remain in bootstrap module (#137 scope)
- Updated `tests/kanban_js.py` with `load_dashboard_js()` helper; dashboard HTML/JS tests reference new module paths
- Quality gates: ruff, mypy, pytest (830 passed, 84.76% coverage)
- Closed #136

Remaining queue: #137

## #137 — Extract Profile Manager client logic into static module
- Extracted Profile Manager into `src/web/static/js/profileManager.js` with `createProfileManagerController` factory
- `dashboardApp.js` now imports profile manager and wires `getActiveResume` to job search settings and `onActiveResumeChanged` to board variant rendering
- Updated `tests/kanban_js.py` and `tests/test_dashboard_modules.py` with new module path and static serve test
- Quality gates: ruff, mypy, pytest (830 passed, 84.71% coverage)
- Closed #137

Remaining queue: none — PRD #114 complete

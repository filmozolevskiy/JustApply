# Ralph Loop Progress — PRD #114

Started: 2026-07-02
Parent PRD: #114 (Kanban Dashboard Maintainability)
Queue: 7 ready-for-agent issues (#131–#137)

## #131 — Move Job Tracker Database init to FastAPI startup and add health smoke test
- Removed module-import `init_db()` from `src/web/server.py`; runs in startup hook alongside Batch Poller (skipped under pytest via `PYTEST_CURRENT_TEST`)
- Added `tests/test_server_lifecycle.py`: import does not init DB, `/api/health` smoke test, startup creates jobs table
- Quality gates: ruff, mypy, pytest (820 passed, 84.47% coverage)
- Closed #131

Remaining queue: #132–#137

## #132 — Remove sys.path hacks from dashboard web entrypoints
- Removed `sys.path.insert` from `src/web/server.py` and `src/web/run_dashboard.py`; package-relative imports already resolve via `python3 -m src.web.run_dashboard`
- Simplified `run_dashboard.py` to use `Path(__file__).resolve().parents[2]` for reload dirs
- Added `test_web_modules_do_not_mutate_sys_path` in `tests/test_server_lifecycle.py`
- Quality gates: ruff, mypy, pytest (821 passed, 84.67% coverage)
- Closed #132

Remaining queue: #133–#137

{
  "mcpServers": {
    "git": {
      "command": "bash",
      "args": [
        "-c",
        "export $(grep -v '^#' .env | xargs) && exec ./.venv/bin/mcp-server-git --repository ."
      ]
    }
  }
}
